[project]
name = "iphone-cua"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "verifiers>=0.1.4",
    "mlx-vlm>=0.3.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.0",
    "ruff>=0.14.1",
]
